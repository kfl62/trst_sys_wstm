:ruby
  grns = @object.dlns.empty?
  tdat = grns ? mat(@object,'model_name_grn') : mat(@object,'model_name')
- xhr_hidden_data({dialog: "delete",title_data: tdat,oid: @object.id,js_ext: 'desk_invoice'})
%form{action: "/sys/#{@path}", method: "delete"}
  %table
    %thead
      %tr.pdb-05
        %td
          - if grns
            - detail_for 'warning w-40rem ta-ju', t('intro.show.desk_invoice.grn',doc_name: @object.doc_name,id_date: l(@object.id_date,format: :trst),client: @object.client.name[2])
          - else
            - detail_for 'warning w-40rem ta-ju', t('intro.show.desk_invoice.main',doc_name: @object.doc_name,id_date: l(@object.id_date,format: :trst),client: @object.client.name[2])
    = haml :"/#{@path}/_show"
    %tfoot{class: 'ta-ce'}
      - tr_buttonset ['delete','cancel']
